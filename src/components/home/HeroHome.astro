<section class="hero">
    <h1 class="hero-heading">
        เอาไว้ก่อนเดะมาทำต่อ
    </h1>
</section>

<style>
.hero {
    width: 100%;
    height: 100px;
    background-color: var(--color-primary);
    color: var(--color-primary-dark);
    padding: var(--spacing-between);
}

.hero-heading {
    text-align: center;
    justify-content: center;
    display: flex;
    flex-direction: column;
    color: var(--color-primary-dark);
}
</style>