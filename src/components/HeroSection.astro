<<<<<<< HEAD
---
import Container from "./ui/Container.astro";
import Button from "./ui/Button.astro";
import { Image } from "astro:assets";
---

<section class="hero">
  <Container>
  <h2 class="hero-heading">
    New
  </h2>
    <div class="hero-content">
      <!-- Featured Image -->
      <div class="hero-image">
        <Image
          src="/photos/111.jpg"
          alt="Cozy Danish-style house with warm lighting"
          class="hero-img"
          width="839"
          height="424"
        />
      </div>
      <div class="hero-text">
        <div>
          <h1 class="hero-title">Candlelight Haven</h1>
          <p class="hero-description">
            The Hygge Home, rooted in Danish Hygge (pronounced "hoo-gah"),
            creates a cozy sanctuary with soft lighting, natural textures, and
            minimalist decor. Cozy essentials like cushions and rugs enhance the
            vibe. Hygge encourages slowing down and finding joy in everyday
            moments.
          </p>
        </div>
        <div class="hero-cta">
          <Button href="/articles" variant="primary" size="sm">
            Read More
          </Button>
        </div>
      </div>
    </div>
  </Container>
</section>

<style>
  .hero-heading {
    font-size: var(--font-title);
    font-weight: bold;
    color: var(--color-primary-dark);
    padding-top: 40px;
    padding-bottom: 40px;
  }

  .hero {
    background: var(--color-primary);
    padding: var(--spacing-3xl) 0;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-between);
    align-items: center;
  }

  .hero-text {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    height: 424px;
  }

  .hero-title {
    padding-bottom: 20px;
    font-size: var(--font-title);
    font-weight: bold;
    color: var(--color-primary-dark);
    line-height: 1.1;
  }

  .hero-description {
    font-size: var(--font-description);
    color: var(--color-primary-dark);
    line-height: 140%;
  }

  .hero-image {
    position: relative;
  }

  .hero-img {
    border-radius: var(--border-radius-md);
  }

  @media (max-width: 768px) {
    .hero-content {
      grid-template-columns: 1fr;
      gap: var(--spacing-xl);
    }

    .hero-title {
      font-size: var(--font-size-4xl);
    }

    .hero-img {
      height: 300px;
    }
  }
</style>
=======
---
import Container from "./ui/Container.astro";
import Button from "./ui/Button.astro";
import { Image } from "astro:assets";
---

<section class="hero">
  <Container>
  <h2 class="hero-heading">
    New
  </h2>
    <div class="hero-content">
      <!-- Featured Image -->
      <div class="hero-image">
        <Image
          src="/photos/jeck.jpg"
          alt="New article"
          class="hero-img"
          width="839"
          height="424"
        />
      </div>
      <div class="hero-text">
        <div>
          <h1 class="hero-title">Transport Economic</h1>
          <p class="hero-description">
            Thi thesis would like to research, learn about the Economic that isn't just microeconomic and marcoeconomic, it has many factors that we don't know one of thwm I would like to do about transportation
          </p>
        </div>
        <div class="hero-cta">
          <Button href="/articles" variant="primary" size="sm">
            Read →
          </Button>
        </div>
      </div>
    </div>
  </Container>
</section>

<style>
  .hero-heading {
    font-size: var(--font-title);
    font-weight: bold;
    color: var(--color-primary-dark);
    padding-top: 40px;
    padding-bottom: 40px;
  }

  .hero {
    background: var(--color-primary);
    padding: var(--spacing-3xl) 0;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-between);
    align-items: center;
  }

  .hero-text {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    height: 424px;
  }

  .hero-title {
    padding-bottom: 20px;
    font-size: var(--font-title);
    font-weight: bold;
    color: var(--color-primary-dark);
    line-height: 1.1;
  }

  .hero-description {
    font-size: var(--font-description);
    color: var(--color-primary-dark);
    line-height: 140%;
  }

  .hero-image {
    position: relative;
  }

  .hero-img {
    border-radius: var(--border-radius-md);
  }

  @media (max-width: 768px) {
    .hero-content {
      grid-template-columns: 1fr;
      gap: var(--spacing-xl);
    }

    .hero-title {
      font-size: var(--font-size-4xl);
    }

    .hero-img {
      height: 300px;
    }
  }
</style>
>>>>>>> dd7a9be (อปเดตหนาเวบ)
